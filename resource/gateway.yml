app:
  ip: 127.0.0.1
  port: 14000
  mode: debug # release
  routes:
    bbs: http://localhost:4000
    app: http://localhost:3000
    uaa: http://localhost:5000

log:
  filename: logtmp/gateway.log
  maxSize: 500
  maxBackups: 3
  maxAge: 3
  level: "debug"
  stdout: false